# Pin every content-addressed artefact to LF so its committed bytes match the
# SHA-256 recorded in model_metrics.json on every platform (the integrity gate
# re-hashes these files, so a checkout that rewrote line endings would fail it).
models/*.json text eol=lf
