apiVersion: v1
kind: Service
metadata:
  name: salary-api
  labels:
    app: salary-api
spec:
  selector:
    app: salary-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
