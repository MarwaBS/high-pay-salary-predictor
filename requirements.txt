# ── Core Data Stack ────────────────────────────────────────────────────────────
pandas>=1.5.0
numpy>=1.21.0

# ── Visualization ──────────────────────────────────────────────────────────────
plotly>=6.9.0            # Interactive charts in Streamlit

# ── Machine Learning ───────────────────────────────────────────────────────────
scikit-learn>=1.1.0
xgboost>=1.7.0

# ── Statistical Analysis ───────────────────────────────────────────────────────
scipy>=1.9.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.20.0
httpx>=0.27.0            # HTTP client the dashboard uses to call /predict on the API

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.22.0  # ASGI server for FastAPI

# ── API Security & Observability ──────────────────────────────────────────────
slowapi>=0.1.9                 # rate limiting for FastAPI
limits>=3.6.0                  # storage backend for slowapi
prometheus-fastapi-instrumentator>=7.0.0  # /metrics endpoint + auto HTTP metrics

# ── API Caching ───────────────────────────────────────────────────────────────
# Optional: only connects if REDIS_URL env var is set. The API degrades
# gracefully to a no-op cache if Redis is unavailable (see api/cache.py).
redis>=5.0.0

# ── Configuration & Utilities ─────────────────────────────────────────────────
pyyaml>=6.0              # config.yaml loading

# ── Development & Testing ─────────────────────────────────────────────────────
jupyter>=1.0.0
pytest>=7.2.0
pytest-cov>=4.0.0            # coverage reporting (make coverage)
ruff==0.15.16                # fast linter + formatter (replaces flake8 + black + isort)
mypy==2.1.0                  # static type checker (make type-check)
pandas-stubs>=2.0.0          # pandas type stubs so mypy resolves DataFrame/Series
pre-commit>=3.6.0            # git pre-commit hooks (pre-commit install)

# ── API-only runtime pins ─────────────────────────────────────────────────────
# requirements-api.txt holds a lean, exact-version list for the API Docker
# image. This requirements.txt remains the loose dev/notebook environment.
# Regenerate requirements-api.txt with `uv pip compile` when updating
# fastapi / pydantic / xgboost / prometheus-fastapi-instrumentator.
