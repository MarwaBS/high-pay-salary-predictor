# Exploratory-analysis dependencies. NOT installed in production.
#
# These are imported only by notebooks/. Keeping them in requirements.txt made
# every clone, every Docker build and every pip-audit run carry packages the
# serving path never imports. Install with:
#   pip install -r requirements-notebooks.txt

geopandas>=0.12.0            # choropleths in us_high_income_jobs_mapping.ipynb
lightgbm>=3.3.0
matplotlib>=3.11.1
openpyxl>=3.1.5              # Excel I/O for the raw BLS OEWS workbook
optuna>=3.0.0                # HPO exploration in ARCHIVED_04_salary_prediction_model_v1.ipynb
pyshp>=2.3.1
seaborn>=0.13.2
shap>=0.51.0
statsmodels>=0.14.6
