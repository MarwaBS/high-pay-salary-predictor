# Trivy container-scan ignores.
#
# Add CVE / GHSA IDs here only with inline rationale, and only when a breaking
# fix is not feasible. Trivy does not honour pip-audit's ignore list, so a
# Python package CVE surfaced by a container scan needs its own entry here.
