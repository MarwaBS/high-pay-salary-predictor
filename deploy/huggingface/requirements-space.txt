# ── HF Space dashboard extras — layered over requirements-api.txt ───────────
#
# Floors, not pins: the Space image resolves these at build time on its
# python:3.11-slim base. The exact pins in requirements-dashboard.txt are a
# freeze of the 3.12 dashboard Docker image and do not install on 3.11, so
# they cannot be reused here. This file exists so the Space's dependency set
# is covered by the CI `pip-audit` gate (dedicated step, 3.11 matrix leg).
httpx>=0.27.0
matplotlib>=3.5.0
plotly>=5.10.0
scikit-learn>=1.1.0
scipy>=1.9.0
seaborn>=0.11.0
statsmodels>=0.13.0
streamlit>=1.20.0
